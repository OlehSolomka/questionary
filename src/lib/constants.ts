export const SCREEN_TYPES = {
  SINGLE_CHOICE_TEXT: 'single_choice_text',
} as const;

export const ROUTES = {
  QUESTION: 'question',
  RESULT: '/result',
  INFO: '/info',
  ROOT: '/',
} as const;

export const DEFAULT_KEY = 'default';
